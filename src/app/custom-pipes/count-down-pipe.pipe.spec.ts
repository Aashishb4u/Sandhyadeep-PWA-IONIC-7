import { CountDownPipePipe } from './count-down-pipe.pipe';

describe('CountDownPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CountDownPipePipe();
    expect(pipe).toBeTruthy();
  });
});
